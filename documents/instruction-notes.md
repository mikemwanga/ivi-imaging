
- Choose approximate cell diameter in pixels carefully, as it is also used to calculate neighbours
- Neighbour count is only comparable between samples where the same diameter was used
- Channels are one-indexed for the user, but zero-indexed in the code (i.e. channel 1 = image[0])